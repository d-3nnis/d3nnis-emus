#include "chip8.hpp"

// static SDL_Window *window = NULL;
// static SDL_Renderer *renderer = NULL;

void initDisplay(void) {
    SDL_Log("hgello");
    // SDL_Init(SDL_INIT_VIDEO);
    // SDL_WindowPtr win(SDL_CreateWindow("Test", SDL_WINDOWPOS_CENTERED,
                                       // SDL_WINDOWPOS_CENTERED, DISPLAY_WIDTH, DISPLAY_HEIGHT, 0));
    // SDL_Delay(2000); // Show for 2 seconds
    // SDL_Quit();
}

void startChip8Emu(void) { initDisplay(); }
